spring:
  datasource:
    url: jdbc:mysql://localhost:3306/car_parking_management_system?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true&serverTimezone=Europe/Sofia
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: never
  mail:
    host: smtp.gmail.com
    port: 587
    username: caraboychev@gmail.com
    password: ${MAIL_APP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

server:
  port: ${port:8081}
app:
  guest-user-id: 2
